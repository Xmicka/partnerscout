# Partner Scout

This repository contains the Partner Scout app for managing channel partner discovery, vetting, and outreach.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env` and add your API key.
3. Run the app:
   `npm run dev`
